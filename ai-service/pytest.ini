[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
markers =
    needs_model: requires trained artifacts in app/artifacts/ (skipped if absent)
